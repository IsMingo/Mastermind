using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Random = UnityEngine.Random;

public class SoluceGenerator : MonoBehaviour
{
    private new Color[] colorsRange = new Color[4] {Color.cyan, Color.red, Color.white, Color.yellow}; 
    private new int[] solution = new int[4] ;
    private new ArrayList _arrayList;
    public GameObject ball;

    void Start()
    {
     solution ={Random.Range(colorsRange[]),Random.Range(colorsRange[],Random.Range(colorsRange[],Random.Range(colorsRange[]}   
    }

    // Update is called once per frame
    private void Update()
    {
        Victorytest()
    }
    

    private void FixedUpdate()
    {
        
    }

    protected void ColorsAssociation()
   {
       
   }

   void ColorGenerator()
   {
        
   }

   void Victorytest()
   {
       
   }
    
    
    
    
    
    
    
    
}
